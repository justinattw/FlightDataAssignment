package Data

import org.scalatest.FunSuite

/**
  * In our implementation, there are some assumptions we made to the data (see Solutions.Question3). A testing framework
  * should cover test cases to ensure the data is in the correct and expected format, such that the behaviour of the
  * implemented methods for analysis can be anticipated.
  */
class PassengersDataTest extends FunSuite {

  /**
    * The only thing we really need to test is if Id is null, I suppose
    */

}
